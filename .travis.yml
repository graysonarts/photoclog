language: python
python:
    - 2.6
    - 2.7
before_install:
    - sudo apt-get update
    - sudo apt-get install libsqlite3-dev
    - sudo sh install_qt.sh
install: pip install -r requirements.txt --use-mirrors
script: sh tests.sh
branches:
    only:
        - master
        - develop
notifications:
    email:
        recipients:
            - russell.hay@gmail.com
        on_success: change
        on_failure: always
